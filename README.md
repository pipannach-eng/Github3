# Dashboard การยกระดับการประเมินผ่านเกณฑ์

ไฟล์เว็บ:
- `index.html`
- `styles.css`
- `app.js`
- `data.js`

ไฟล์ข้อมูลถูก generate จาก:
- `C:\Users\User\Documents\G3\prepared_dataset_ml\ml_dataset_workbook.xlsx`
- sheet `improvement_indicators`

วิธีเปิดใช้งาน:
1. รัน `build_dashboard_data.ps1` เมื่อต้องการอัปเดตข้อมูล
2. เปิด `index.html` ในเว็บเบราว์เซอร์

หมายเหตุ:
- Dashboard ใช้ `center_code_anon` เท่านั้น
- เป็นเครื่องมือช่วยคัดกรองและจัดลำดับ ไม่ใช่ผลรับรองอย่างเป็นทางการ
